version: '3.8'

services:
  web:
    build: .
    container_name: flat-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - SQLITE_DB_PATH=/app/data/sqlite.db
